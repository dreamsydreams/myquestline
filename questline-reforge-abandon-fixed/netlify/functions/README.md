# Netlify Functions

Empty for now. Phase 3 will add the stubbed Campaign Generation Engine
(Book II, Ch.9) here behind a clean abstraction, so the real AI call can
be dropped in later without restructuring anything.
